# Independently invented supervisor fixtures

Certificate generated independently for parser tests; its private key existed only in generator process memory and was never persisted. No endpoint or runtime is qualified.

The public certificate was generated independently for this fixture family.
It is not derived from an actual application or an external database model.
The configuration is a parser example and cannot qualify a runtime or endpoint.
