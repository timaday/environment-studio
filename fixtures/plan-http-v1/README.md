# Independently invented plan command

This shape-only command uses independently invented slots, values and IDs.
It is not a live plan, complete placement, publication or export authority.
No actual application model or runtime data was used.

`preview-digest.json` is an independently invented framing example, including a
revision beyond JavaScript's exact integer range. Its pins are not publications
or observations. Run `python3 fixtures/plan-http-v1/preview-oracle.py` from the
repository root to check the three exact native-framed digests independently.
