# Independently invented package shape example

This family tests planned schemas and execution framing only. It is not an
archive or an executable artifact. Publication/observation fingerprints are
invented, and SQL/instruction member digests deliberately have no matching
files. No execution authority follows from schema acceptance. Every name, XML
character and identity was invented independently; no actual model was used.
