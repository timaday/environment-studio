# Independently invented native v2 family

These declarations and the digest oracle were invented from scratch for generic
compiler and future disposable mock database tests. No actual application model,
XML, table, locator or private input was consulted or transformed.

The two alternative bindings describe the same invented logical types. Future
planner tests can create a second palette with fresh values and retarget one
glyph reference, retaining other glyphs and unrelated XML. This fixture does not
claim a database has been observed or a planner/writer has executed that case.

`digest-oracle.py` independently implements the frozen byte framing in Python,
without importing Java compiler code. `expected-digests.json` records its output.

`child-property-digest-oracle.py` adds the explicit child locator to the invented
Postgres binding and independently frames only that binding's additional mechanism
dependency. `child-property-expected-digests.json` preserves the original logical
and Oracle binding digests. Run both Python scripts from any directory; their
printed JSON must equal their corresponding expected files. Neither imports Java
implementation or real configuration.

`historical-direct-snapshot.json` was captured using the pre-extension native
compiler-2 and historical codec from this same invented definition, object 81,
workspace publication revision 2, with deny policies for every document. It is
frozen compatibility evidence, not a runtime input. SHA-256:
`5e953fca9954ca69b6dd2b179978f245dbc17c74de06ab0cdeefe07808272ba7`.
The publication digest is
`e8de0efc4cee2573e225060ccd7ff1f372ff48a1d96ae8a7ada34896e2d2dfc2`.
Do not regenerate this fixture with the new codec to make a failing replay pass.
The history tests require exact byte roundtrip and the unchanged publication pin.
