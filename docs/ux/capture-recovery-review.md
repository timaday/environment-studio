# Capture save-response recovery proposal

Approved with listed corrections: complete desktop v1 and narrow v3.
User response “Approve both with listed corrections”, 11 September 2026,
call_e8R7H2ZunlkEv5VSo8dfcnd4. Existing six v4
mapping/review/saved approvals remain unchanged. This proposal covers an unknown
save response in the original session with the captured profile available.
It does not approve unseen expired-session or changed-context recovery layouts.

The authorized independently invented profiles-v3 workflow returned the three
items and two relationships shown. An actual draft PUT completed, then the test
aborted delivery to the browser. The browser reported NETWORK_UNCERTAIN while
the existing renderer misleadingly retained “Captured · Not saved”. Actual
historical readback and exact-command replay both matched draft revision 1.
That test knowledge is deliberately absent from the unknown-response screen.

Source checkout: e0ab0af3dbe46619acc4d12cffb4667db30ca386 plus preserved renderer
WIP; eligible fixture ancestor 820837cd9c0249bc5646f52733ea2a2b40b559e5.
External observation: es-capture-save-recovery-observation-20260911/observation.json.
The isolated test uses HostedBrowserHarness, V3WorkflowHttpTestConfiguration and
V3WorkflowStorageFixtures with test-only observation/publication prerequisites.
No production compiler publication, actual database connectivity, admission or
release qualification is inferred. Credentials/session values were omitted from
the observation, images and prompts. Earlier probe setup errors remain external.

The operator sees one “Save not confirmed” alert, the original read-only captured
profile, a Retry save action and Back to plan. Existing pending-save state keeps
the exact command; retry must use it once per explicit action. Editing remains
unavailable until settlement. The renderer connection is planned, not implemented
or proven by these images. Successful confirmation returns to the already approved
Draft saved layout; it never implies publication. Subsequent whole/partial reuse
remains available under its existing contracts.

Required presentation corrections, explicitly included in the approval request:
use actual SVG branding and flat committed Midnight tokens; body/help text at
least 14–16 CSS pixels and all actionable controls at least 44 CSS pixels high;
natural page scrolling. The generated narrow Retry save is about 70 raster pixels
high at 783 pixels wide (about 35 CSS pixels at 390 CSS width), so it must grow.
Desktop retry is about 43 raster pixels at 1505 width (about 41 CSS pixels at
1440 CSS width), also requiring growth. Generated fonts/shading are approximations.
Preserve the narrow stacked item list, one collapsed help disclosure and compact
header/context. Keep the explanation below the summary as plain supporting text,
consistent with desktop; the generated narrow information strip is not another
status announcement. No pixel identity is claimed.

Functionality: actual lost-response/save/readback/API replay observed; UI retry
connection pending. Accessibility: generated proposals unverified; required size,
keyboard, focus, announcements and reflow checks remain. Visual fidelity:
DIFFERENCES_REMAIN until approved corrections are implemented and compared at the
recorded viewports. First two narrow proposals were rejected locally for compressed
layout/table composition; originals remain external, never approval references.

Exact image dimensions/hashes and observation hash are in
[capture-recovery-approval.json](reference/capture-recovery-approval.json).
Every image has the outside-frame caption “Design proposal — synthetic test
environment.” It is not product UI.
