# Values and Validation approval packet

Status: pending image approval. User `call_qBBkVB58oX93NDpKBV9qXZ4D` authorized
actual reviewed mock-workflow content for these previews. Exact images, hashes,
dimensions and corrections are in `reference/values-validation-approval.json`.
These are presentation proposals, not implemented views or release evidence.

The source is the `profiles-v3` HostedBrowserHarness at
`505f815552d4439220cc17e5c1ce120cff0df535`, with its existing
V3WorkflowHttpTestConfiguration, V3WorkflowStorageFixtures and independently
invented V3WorkflowHttpWitnesses. The isolated run used actual HTTPS/OIDC,
operation-scoped inspection, plan commands, bindings, validation and rule paging.
It used the existing test-only definition publication/observation witnesses;
neither admits production publication or proves database connectivity.
The process exited successfully and reported complete cleanup. No credentials,
session responses, raw XML or secret values enter the images or prompts.

Values shows the actual revision3 bindings after explicit retention of the
returned item reference. The tone field is unresolved; beta is an entered local
choice, not an acknowledged change. The real bind-field command subsequently
returned revision4 and bindings readback confirmed alpha → beta. An earlier
attempt to bind an unselected item was correctly refused with422 INVALID_REQUEST;
the design does not imply that materializing alone selects an item for editing.

Validation shows that revision4's actual complete target has seven PASS and three
UNKNOWN required checks. Client capability, content policy and review remain
UNKNOWN. Both returned co-occurrence rules pass; this never overrides the required
checks or makes export available. The empty application-rule list is actual.

Functionality mapping:

- Plan context, item inventory and current/target comparison: plan summary,
  entity pages and binding pages; current values remain read-only.
- Values edit: existing `useV3TargetValues` over original-owner command lifecycle;
  retain/select eligibility comes from explicit draft state, not field names.
- Validation: `useV3Validation`; full backend summary, exact fingerprint and
  bounded pages, fresh summary correlation, no client export authority.
- Back/review links: local journey navigation retaining plan context.
- Details: disclosures for existing plan/session/evidence/mapping information;
  no invented names, technical meaning or credentials.

Review findings: the first narrow Validation proposal squeezed five rule columns
across a phone. v2 replaces them with stacked read-only cards. Values desktop v2
removes input styling from captured/plan cells. Remaining explicit corrections:
actual SVG, flat tokens, periwinkle primary actions,14–16px body,44px controls,
labelled desktop Item selector, compact consistent narrow shell and stacked title
actions. Design matching, browser functionality and accessibility remain untested
until implementation. No pixel identity or independent UX review is claimed.

Approval covers only the shown edit and validation states. Recovery, applied
acknowledgement, retention prerequisites and alternative input-state compositions
need applicable approval before their affected rendering is implemented.
