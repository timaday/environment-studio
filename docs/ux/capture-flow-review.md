# Capture operator flow — proposal v4

Six complete screen proposals use actual results from the user-authorized,
independently invented profiles-v3 test workflow. Exact images, dimensions,
hashes and outstanding corrections are in `reference/capture-flow-approval.json`.
The user approved these six states with the explicitly listed corrections on
11September2026 (call_qicEZ3O3VHQnZ5xCc0tk42n6). Prior component sheets remain
revision-requested and unseen recovery states remain outside approval.
Every image has “Design proposal — synthetic test environment.” outside its
application frame; this caption is not production UI.

## Source and actual observation

Source `dd4d007683dcc2ebefd6694f3bc321ad10fea6bc` is a verified descendant of the
explicitly authorized fixture base820837cd9c0249bc5646f52733ea2a2b40b559e5.
The existing HostedBrowserHarness, V3WorkflowHttpTestConfiguration,
V3WorkflowStorageFixtures and v3-profile-capture.spec.ts supplied the workflow.
An external copy adds only allowlisted result recording after all assertions;
no response interception, fabricated payload or production demo fallback.

The current five Java test helpers compiled into14 isolated classes against
1,583 hash-verified inherited runtime files. Current production Java matched
the previously qualified local runtime source. A private fresh RAM SQLite/TLS
workspace used loopback18445/18446, one browser worker and its own outputs.
TypeScript e2e compilation passed. Actual API/browser journey:1PASS2.5s;
owned cleanup and inherited hashes unchanged. No screenshot, trace, credential,
session contents, request identity or browser assertion dump was exported.
Safe result projection SHA256:
`5ac6522054e1533375c5b9d2fe6d251a11e23a607dfe8706638b66a74de0a2a3`.
External runner/result/observation: `es-capture-design-observation-20260911`
under `/home/tim/.tmp`. A path captured under the field failureLocations is
merely the successful runner's test-location line; testExit0 and summary establish
that it was not a failure. Do not infer a failed test from that field name.

| State | Authoritative content used |
| --- | --- |
| Mapping | Actual two inventory pages return3 existing items of declared type item; explicit test slot-1/2/3 and Neutral1/2/3 mappings, profile identifier mock-profile/revision1. These are entered mappings, not defaults. |
| Captured review | Actual capture response:3 physical entities,2 link relationships and required id input definitions. Exact source disclosed separately; no donor values retained. Profiles list still empty and plan summary unchanged before save. |
| Draft saved | Separate real PUT returns draft workspace revision1; historical GET exactly equals saved source/definition/receipt. Current read after re-login also matches; foreign owner404 and final canary check pass. No new publication witness is installed. |

The fixture installs an explicit matching historical definition into SQLite and
uses test-only observation/publication witnesses. These prerequisites do not
establish production compiler publication, actual database connectivity, client
qualification or release readiness. “Draft saved” describes this verified test
operation only. No published or production-qualified state is claimed.

## Interaction and remaining work

Prerequisite gating follows the corrected journey: absent plan or invalid
inspection shows recovery and no mapping form. Once complete inventory is loaded,
all required mappings are explicit. Desktop displays rows; narrow proposes
expandable rows with entered labels/IDs retained. A collapsed row is not deselected.
For larger inventories use20-row pages, preserve all entries and expose incomplete
mappings across pages; the3-item fixture does not invent a pagination control.

Capture produces reviewable source; edit mappings invalidates captured source.
Save is explicit and uses the existing exact-command replay owner. Uncertain
save/recovery is still governed by the implemented state contract and needs its
own applicable presentation approval; these successful-state images do not
approve that unseen state. Saved disclosures expose actual immutable revision,
items/relationships and source. Publication and whole/partial reuse remain
separate, with unselected siblings preserved. Back to plan retains existing Current/Target document inspection and
Raw/Formatted access. Full many-document navigation and Placeholders with the
concrete binding rail remain required work; this capture proposal does not
claim those missing renderer capabilities are complete.

Every visible command is either an existing state/API operation or planned local
rendering over that state: context/source disclosures, row expansion and navigation.
No renderer is implemented in this proposal branch. Preserve current owner/context
invalidation and pending-save lifetime rather than adding another state engine.
Exact technical handles/definition/plan/receipt references stay accessible in
labelled details, without becoming the primary task labels.

## Verification and explicit visual corrections

Functionality: the isolated API/persistence content workflow passed; this is not
a browser execution of the proposed capture renderer.
Accessibility: unverified. Several generated inputs/buttons measure below44CSSpx
when scaled to the intended390px viewport; no keyboard/zoom/focus result exists.
Visual fidelity: DIFFERENCES_REMAIN, no pixel-identity claim. Use the original
SVG and flat committed tokens. The revised narrow review restores periwinkle primary/value-exclusion copy
and collapses the previously empty help panel. Desktop review summary still
has input-like borders despite being read-only. Compact header/context/progress,14–16px text and44px controls require
explicit correction with natural scrolling; no permission to scale text down.

User approval must identify the six state/viewport images and any accepted
corrections. Theme agreement and historical initial-form approval do not cover
these populated screens or pending Definitions designs. Implementation and
measured browser verification remain separate obligations after approval.

Independent fixed review verified all fixture/image hashes and state content.
Two additional findings were accepted: correct the unsupported Placeholder
access claim (now fixed above), and collapse narrow review help rather than
show an empty description (revised image hash in manifest). The remaining
size/token/read-only/context corrections still need explicit approval.

Approval received: “Approve these states with the listed corrections.”
Exact scope/hashes are in the manifest. This authorizes implementing these
presentations with the listed corrections, not claiming renderer verification,
accessibility compliance, literal pixel identity or unseen state approval.
