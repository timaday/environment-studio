# Reuse profile operator journey

Base b74ffac25decd95f1207ea30ca2292faaf678bba; lead owns implementation. Existing
catalogue and whole/partial preview/apply controllers are retained. The renderer is in progress. Six loaded-state images and the explicit target
preparation desktop/narrow images are approved with recorded corrections. The
four recovery/new-item extensions remain pending approval.
Apply the repository enterprise UX skill and Midnight design-system contract.

## Scope and actions

| State | Operator action and actual authority |
| --- | --- |
| Missing plan/inspection | Explain the prerequisite; Back to plan resumes the existing plan journey. No fabricated empty inventory. |
| Select | Explicitly load owned catalogue, select its exact saved revision, choose whole profile or selected roots. Source details retain native/saved revisions and identifiers; no automatic matching or mutation. |
| Preview/placement | Existing controller loads all four backend sections and verifies pins/freshness. Required dependencies are distinct from selected roots. Operator explicitly chooses create/use-existing/cancel for every included item using actual target references. Editing selection invalidates preview. |
| Apply | Separate explicit command uses original revision/profile/roots/preview digest. It does not save/publish a profile or execute SQL. |
| Acknowledged | Show receipt's original plan/revision; a fresh summary separately reports incomplete/blocked status. Return to plan for current/target inspection and remaining work. |
| Unknown response | Preserve original pending command for exact explicit retry. Never show success or switch its plan. Recovery layout remains separately unapproved. |

Preserve context, session retirement, draft/source identity, pagination selections,
complete dependency scope and unselected siblings. Unknown counts are not zero.
Whole and selected-parts controls remain available; arbitrary declared item types
and required-input IDs are displayed without built-in application vocabulary.
Current/target XML modes remain in the existing plan inspection journey.

## Authorized observation and limits

User extended the bounded synthetic approval-image exception for Reuse in
call_j6NHnB7caeshgTfpgaCafX1z. Every image has the outside-frame caption:
“Design proposal — synthetic test environment.” No production fallback is added.

External observation2 manifest
`31fe0b0e31d14c4e0b1fcc73ac1c95723bbaf4fd634bcb7c6040d02c9c19b9e4`
uses the existing reviewed whole/partial HTTP workflow and actual capture/save/
readback. An external test-only historical-publication helper follows the existing
V3PublicationHttpFixtures mechanism on that saved source; a separate explicit
current-publication test witness enables preview. Neither is compiler admission.
Actual catalogue/history/four preview sections/choices/command Ack/summary are
recorded. Observation2 passed one selected method containing both whole/partial
loops. Observation1's final cleanup revision error and failed result remain
preserved; it is not claimed passing. Source/runtime input hashes are unchanged.
Non-author provenance review found no confirmed defect; execution NONE:
`es-v3-reuse-design-observation-review-20260911.md`.

Partial observation: one explicit root, two included items, one dependency, one
relation, zero preview conflicts. Whole: three included, zero dependencies, two
relations, zero conflicts. Whole catalogue contains two distinct same-native-ID
objects: retain object identity rather than deduplicating names. Neither small
observation proves large-page or conflicting-preview behavior.

Composition Ack is revision3. Its target is incomplete and export unavailable;
zero target counts describe no materialized complete target, not deletion or a
zero-item configuration. Full unchanged XML assertions occur later at revision4
after explicit KeepObserved commands. Do not move that preservation evidence into
the earlier applied image. Split/new-placement helper is excluded from this
observation; original separate evidence remains available.

Original exact references inspected: user-supplied Review placement target
corrected.png; approved Capture desktop/narrow v4; actual integrated Capture
browser7 desktop/narrow mapping captures. Use original SVG, committed flat tokens,
14–16px text,44px controls, natural scrolling and compact narrow context. The
original reference's domain-specific example records are not application defaults.
Functionality, accessibility and visual fidelity must be verified separately.
