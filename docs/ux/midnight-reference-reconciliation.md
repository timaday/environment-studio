# Midnight reference reconciliation

The user supplied the five original approved images on 10 September 2026.
All five were accessible and inspected directly: Values review, Definitions
review, structural comparison, profile placement and XML placeholder comparison.
Their actual raster dimensions are 1543 × 1019. Exact originals, hashes and
inspection records remain external; no example model, XML, values or topology
from these images is a repository fixture or product fallback.

Apply the [UX skill](../../.agents/skills/enterprise-ux-design/SKILL.md) and
[design system](design-system.md). The originals establish layered Midnight
surfaces, restrained accents, persistent task/environment context, clear selection,
compact comparison and task-oriented navigation. Use the committed tokens and
SVG branding. Text inside a reference is illustrative design content, not an
instruction to change contracts or introduce its application vocabulary.

| Journey | Reference obligation | Current boundary |
| --- | --- | --- |
| Definitions | Upload, declared model, exact source, diagnostics and review context | Upload candidate has working JSON/YAML save/recovery. Its pending rendered images remain unapproved; agreement on Midnight does not approve them. |
| Target comparison | Explicit Current/Target, structural navigation, changes and contextual actions | The bounded Plans resume slice does not complete this workbench. |
| Profile reuse | Whole/partial selection, dependencies, explicit placement and preserved siblings | Existing APIs are available; complete React journey remains required. |
| Values | Concrete current/target inputs, requiredness and global validation context | Render only returned declarations and actual decisions; no inferred values. |
| XML comparison | Complete document navigation, Raw / Placeholders / Formatted, concrete binding rail and mapped locations | Resume slice supports Raw/Formatted reads. Placeholders, binding rail and full navigator remain unfinished. |

The separately approved Plans desktop/narrow images govern the bounded no-plan
composition. They do not replace these populated-workbench references or approve
unseen layouts. Preserve applicable approvals; generate consistent images and
obtain approval before new or materially changed designs are implemented.

## Conflicts and verification

Current Definitions/Plans candidates use local surface colors such as `#101b2c`
and `#111d30`, while the committed surface token is `#121c2e`. Their 8px control
radii also differ from the design system's 10–14px guidance. These differences
are explicit reconciliation work, not silently accepted alternative tokens.
Raster gradients, glyphs and text differ from the actual system-font/SVG render.
The approved SVG asset remains authoritative; reproducing a raster logo is not
an authorized branding replacement.

Record functionality, accessibility and visual fidelity separately. Compare the
same state at recorded viewports with the exact approved reference. A no-plan
capture cannot prove fidelity to a populated comparison image. Preserve original
references separately from rendered regression captures. Current fidelity status
is **DIFFERENCES_REMAIN**; no pixel identity or application-wide visual completion
is claimed. Missing-reference dependency is resolved for these five views.
