# Nonvisual whole/partial profile reuse state

Candidate base a9d165ee4a5d60a88feb7cd34f9e829e20548d5f. Lead owns this hook,
its tests and the browser-state contract supplement; IDE2 native files are excluded.
No renderer, route, schema, backend authority or approved design changes.

Explicit configuration detaches an immutable profile revision and whole/selected
roots. Preview reads included, dependencies, relations and conflicts completely,
with no partial usable result. Global and per-section comparisons bind pins,
digest, affected derivations and section totals; observation and final full plan
summary must agree. Included slots are unique and dependency/relation endpoints
must belong to them. Java still recomputes composition and verifies complete proofs.

Separate apply requires one explicit create/use-existing/cancel decision per
included slot. It sends only the returned normalized roots, profile, digest and
original revision through compose-profile. No values or replace-draft operation
are generated. A dispatched snapshot keeps its original plan ID across context
changes and uncertain403/413/malformed/network outcomes. Retry cannot substitute
new selection or decisions. Success exposes the original Ack as a receipt, not
a fresh plan summary, complete target, publication readiness or export authority.
Session-required failures and unmount retire owned state; future rendering must
also propagate idle session retirement and display receipt ownership explicitly.

## Actual checks

- Initial setup had a wrong frontend-relative output path, then missing per-worktree
  dependencies. These are not RED. After npm ci, scaffold RED3 runs five behavior
  cases and all five fail (missing complete preview, command, decision refusal,
  mixed-page rejection and uncertain replay).
- GREEN1 passes four, fails one assertion expecting bare CONFLICT instead of the
  existing explanatory failure message. Correcting that oracle retains the code
  and explanation. GREEN2 passes25 including adverse context/pin/session cases.
- Fixed non-author source review finds no confirmed defect. Its positive-page
  coverage limitation is addressed by two additional tests: all four nonempty
  multi-page sections (201/200/205/201 rows,11 requests), and session loss during
  preview. GREEN3/4 pass27; fixed2 supplement accepts this bounded test addition.
- Final full frontend run passes277 tests and59 schema tests. TypeScript/Biome
  check4 passes; production build1 passes with unchanged production hook source.
  Check1/3 initially fail formatting; final test-only formatting is verified
  whitespace-equivalent to the reviewed fixed2 source. No guard/config changed.
- V8 on27 focused cases:114/116 lines,77/84 branches,127/134 statements and19/19
  functions. These are hook instrumentation counts, excluding backend/browser.
- External unchanged25-test control compiles and passes. Six distinct compiled
  faults are detected: mixed sections, revision-only final summary, retired
  generation, wrong-plan replay, uncertain403 unlock and session input retention.
  All six compile exits are0 and test exits1. Mutations stay outside the candidate.

Commands from this worktree's frontend use pinned Node24: npm ci; npm run check;
npm test; npm run build; vitest run src/hosted/useV3ProfileReuse.test.ts; the same
focused run with V8 coverage. External logs use es-v3-profile-reuse-* prefixes in
/home/tim/.tmp; fixed1/fixed2 manifests, mutation result and the fixed review report
retain source boundaries and actual results. No independent reviewer executed tests.

RST investigation challenges mixed completion, changed owners, lost acknowledgement,
partial decision coverage, duplicate/foreign slots and stale final context. Ordinary
and adverse fixtures are independently invented wire models, never real application
material. Partial-reuse sibling preservation remains an existing Java guarantee;
these client tests establish submitted intent, not fresh backend qualification.

No new rendered capture/reuse journey, accessibility or visual-fidelity result is
claimed. Actual browser wiring, shared session lifecycle, independent remote
acceptance, combined Maven/OCI/resource gates and release qualification remain open.
