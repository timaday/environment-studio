# Four-owner large hosted retention prerequisite

The actual HostedPlanService and SessionLedger now pass the scalable fixture with
four retained owners against505f815552d4439220cc17e5c1ce120cff0df535 Java classes.
Each owner retains128 documents,16MiB original XML and16MiB target XML,
10,000 physical/10,000 computed nodes,50,000 memberships and64,000 complete rules.
After every later admission, complete current/target content, original observation
pins, logical/binding pins, physical/derived evidence and provenance are rechecked.

Foreign plan access and a fifth retained plan refuse. Held logout remains
INCONCLUSIVE while the original view owns work; explicit drain completes cleanup.
A fresh session for that owner creates/observes/edits its replacement plan while
the other three remain intact. All five started mock observations close their
transient credentials. Final ledger cleanup completes before the PASS marker.

The external probe adapts the reviewed small hosted control to the unchanged
reviewed scalable shape. Lead source/self-review is recorded; the new adaptation
has no independent review. It adds explicit logical/binding pin checks. Its scope
is retained hosted state/lifecycle: full-wire paging and Raw HTTP are separate,
unchanged small-scope evidence, not claimed at large scale by this probe.

Small control exit0 in0.817s; large exit0 in112.746s. One host JVM used1GiB heap
and two active processors under a180-second external deadline. Peak large RSS was
1,196,664KiB, exceeding the Compose1GiB total allocation; this host result therefore
cannot establish deployment capacity. Application operation deadlines were not
changed. Actual compiler remains Incomplete/MECHANISM_UNQUALIFIED; publication and
database observation are explicit mock ports.

External source, adaptation diff, self-review, commands/input hashes and results:
`/home/tim/.tmp/es-v3-hosted-shape-current-20260911/`. Probe SHA256
`9c88ea5839577fb1cc8e40c976301e88d4d5360667852add292065c6982aaa38`;
large output SHA256
`a76e31ee696db81d5f150152e5b8fbbdd95388175ea2673527f59ea7a1ab2d9d`.
Inputs remained unchanged. All owned JVMs exited and no resource reservation
remains for this host run.

The next check uses the exact container's application classes/libraries and JRE,
Compose-equivalent CPU/memory/PID limits and65% heap. Native/resource closure,
simultaneous incoming observations/encoders, dense child-property shapes and
complete deployment qualification remain open regardless of this prerequisite.
