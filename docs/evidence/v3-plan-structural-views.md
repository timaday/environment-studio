# Hosted v3 structural decisions and binding pages

The five POST routes in [the contract](../contracts/hosted-plan-structural-views-v3.md)
now expose physical relations, explicit draft/containment decisions, eligible
original placements and complete field bindings. The closed dispatcher uses the
existing controlled physical adapter; full original/retained-target proof checks,
pinned output authority and shared scratch/semantic ownership remain unchanged.

## Fixed candidate

Base `e6f079a64ae796f8d9745ad16bc3149dac378bfa`, combined twelve-file manifest
`6025d4ce1f4fee8ebd96eee62815a9fa0380f2d6210560f3bade928dc785e00f`: backend author5,
OpenAPI3 and independent actual HTTP/security4. All hashes checked before/after
full verification. Author manifest
`e4682f8c4950115d2f5b8b31c9f1b6d979b168048acefa26d43c155788e4dddf` changes only the
closed dispatcher, new five-route controller, approved contract and tests/fixture.
Existing reply/physical controller/core remain unchanged. The lead read all5;
the non-author review read root4 and verified all12, finding no blocker.

External author record `es-v3-structural-http-author-evidence-20260910.md` SHA
`01652020ab39f65baae52946cafd5907cdda27cf8d99ac80454168e9e3a99ac5`.
Independent supplement review SHA
`56ee25e1eee7ee653c7eef6aef7b5f70605a091d10ebdb08b8aaa038582ebffb`.
These are local development checks, not qualification of runtime availability.

## Behavior evidence

Actual MockOIDC/CSRF/socket requests use explicit imported, independently invented
publication/observation witnesses and the actual XML/plan adapters:

- Returned Existing references retain physical relation endpoints and binding
  tokens after identity rename. An explicit reference to the original entity
  updates its referring XML value; complete binding counts include both field
  and reference occurrences.
- Explicit unresolved PUBLIC and entered SECRET decisions remain inspectable.
  Repairing only the selected public field preserves the private decision while
  the draft/binding rail masks it. Masked value states remain distinct from
  complete/unavailable occurrence counts and empty/absent public values.
- Original parent coordinates returned over HTTP drive a Fresh creation. Fresh
  current values are unavailable; target values and explicit references resolve.
  Removal has empty decision arrays. Independently written final XML supplies
  the digest oracle across repair, rename, reference update, creation and removal.
- Four one-row target entity pages concatenate to exactly the complete page,
  strengthening the previous slice's page-size oracle. Draft tests also check
  ordered pages and end/beyond-end totals.
- Every new route refuses foreign, CSRF, malformed and stale requests. Closed
  selectors and binding offsets256/257 retain their declared semantics. Author
  controlled tests check V2 refusal before capacity/body on all five routes,
  two-stage admission rollback and zero output after original pin invalidation.
- A real partially received binding request holds scratch/semantic capacity.
  Another owner can read metadata while its view is refused. Original logout
  preserves both legal204/503 schedules; actual worker closure recovers capacity
  and a fresh login cannot restore the retired plan.

Containment examples in this slice cover empty explicit decisions only; existing
lower-level movement tests remain separate evidence. No new movement qualification
is claimed. Credentials/tokens/CSRF stay out of DEBUG output and workspace files.
The information boundary was reviewed: fixtures and all model vocabulary here are
independently invented, with no private model copied or renamed.

## Actual gates

Pinned JDK21.0.12/Maven3.9.16/Node24.20.0 on10 September2026:

| Gate | Result |
| --- | --- |
| Author meaningful RED | 3 selected tests,1 assertion failure,0 errors before dispatch/controller implementation |
| Author final focus | 41 tests,zero failures/errors/skips |
| Bounded mutations | Relation side, placement selector and missing rollback each compile and fail1 assertion/0 errors; exact restored controls pass |
| Independent actual HTTP RED | 3 server assertions,0 errors: missing routes return403 and no held-transfer record exists |
| Independent HTTP final focus | 19 tests:2 controls and17server,zero failures/errors/skips,04:04:50BST |
| G01 `mvn -B -ntp -f backend/pom.xml verify` | 1539:324core/7parser/898server/310supervisor,zero failures/errors/skips; assembly/hostile launcher PASS,04:09:20BST |
| G02 fresh npmci/check/test/build | 40frontend/53schema PASS on exact OpenAPI3; corrected schema RED53 failed only2 missing-route assertions |

The author corrected a test-only ReferenceState type compilation error. The first
schema attempt incorrectly applied root anyOf to a bindingItem test; only its
validator setup changed. The first positive HTTP attempt expected bind-field to
implicitly select an entity. Existing contract correctly refuses that request;
final test asserts422/revision unchanged, then supplies explicit complete retain.
No production gate, selection semantics or schema authority was weakened. None
of these setup/oracle failures is represented as a new production defect.

Full log `es-v3-plan-structural-full1-20260910.log` SHA
`3cba2843320e43d28df2d177db2a71319e975b255125c37813a7a8385ef05133`.
HTTP final log SHA`1dd3e2de2d8ff909d67e66ee03156d6d8e706fde103b40067e40c940553b9744`.
OpenAPI final log SHA`18919e60f7b0d4959ae48e2f8ebd84ac14d196bc63a6e7e745238091ed9a8cd2`.
G00 whole staged provenance/content review, Python11, repository integrity and
staged diff checks PASS. The content checker is a limited pattern guard; the
reviewed seventeen-file diff contains only generic contracts/code and independently
invented fixtures. No remote upload was attempted.

Document/location routes are next, then computed pages, capture/reuse and
validation. The actual compiler still refuses publication. Complete operator UX,
resource/native/export/readback and remote CI/GHCR/HiveForge qualification remain
open; the retained047d1b0 image predates this implementation.
