# Native COPY prerequisite contract — 10 September 2026

Lead-owned contract only, based on567a5ca. The separate native writer retains all
implementation and test ownership. Static client inventory exposed an unsupported
COPY requirement; inventory is not loader, client or release qualification.

The [closed extension](../../backend/tools/guarded-supervisor/docs/privacy-copy-relocation-v1.md)
introduces private recipe version2 for equal-size, versioned COPY initialization
images. Version1 still refuses COPY. Independently synthesize expected bytes from
admitted file/zero/literal/scalar-relocation inputs and compare both endpoints.
Exact symbol starts and complete symbol lengths are required; pointer bytes are
copied without rebasing. Original resource, stop, cancellation and cleanup owners
remain mandatory. Production recipes stay absent and internal success still
ends INSTALLATION without CHALLENGE or FinalAdmitted.

Fixed non-author review identified a contract ambiguity: containment alone did
not explicitly require the complete symbol span or exact donor start. Accepted
and corrected by explicit offset/length equalities before assigning implementation.
Fixed2 non-author verification confirmed the exact two contract hashes and
resolved that finding without further concrete findings. The reviewer inspected
source/evidence only and executed no tests. No production behavior or runtime
qualification is claimed from this documentation work.

Author repository integrity, content guard, whitespace and11 Python tests pass.
There is no behavior RED/GREEN for this contract-only change. The assigned native
implementation must supply actual JNI/FORK RED, independently expected GREEN,
adverse controls and fixed non-author review. Full combined and artifact gates
remain required at their integration boundary; no duplicate full/OCI run occurred.

All repository material is generic mechanism vocabulary and independently invented
acceptance examples. Actual installation inventory and review working artifacts
remain outside the checkout/build context. Effective loader/provider selection,
other COPY forms, TLS/IFUNC, constructors, complete process/exec lifetime, trusted
runtime distributions and whole-resource/client qualification remain outstanding.
