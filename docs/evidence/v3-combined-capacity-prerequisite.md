# Small v3 combined-resource prerequisite — 10 September 2026

A corrected, independently source-reviewed small control passes against fresh
compilation of exact application source875a257d659d3139a2c180cc587531ddcd095c96.
This validates the semantic workload fixture, not deployed capacity or publication.

Actual compiler → XML projection → typed decisions → physical materialization →
final XML reprojection preserves two independently literal current/target documents.
Ten physical and ten computed nodes yield50 memberships,100 contributor links,
45 computed value bytes and64 complete zero-pair rules. Five alpha values become
omega. Full expected XML preserves quote styles, CRLF, comments, entity-reference
input, unmapped CDATA and a supplementary character. Every contributor identity,
role, lexical span, attribute pin, complete origin and retained provenance is
checked against independent expected data. Original sources and separate final
pins remain intact. Actual compilation returns Incomplete with only
MECHANISM_UNQUALIFIED; no Ready or publication witness is constructed.

The first attempt failed Java import setup. Actual compilation then rejected the
proposed maximum-zero cooccurrence fixture. The contract requires a positive
maximum; min0/max1 retains the intended zero actual counts. These fixture/setup
corrections are not production TDD RED or product defects. The static large-shape
feasibility note was corrected accordingly; its large workload is still unexecuted.

Non-author control4 review found incomplete origin/provenance assertions and a
classpath attribution gap. Fixed control5 adds the complete expected origins and
typed provenance, and freshly compiles232 exact committed Java sources plus the
pinned generated parser. All179 generated entries,1015 classes,120 dependency jars
and run logs are hashed. No prior application output directory enters the runtime
classpath; independent ZIP inspection found no application classes in the jars.
A proposed missing rule-target assertion was withdrawn: RuleCheck has no target
field. Existing complete input/key/edge/rule assertions prove the absent shape.

Prepare, javac and JVM steps all exit0. The non-author fixed5 review independently
verified the recorded hashes and corrections; it executed no tests or builds.
The probe SHA256 is
`34644bc80da140e3927c63cdc38ecfeaff6f3bdbd3bed89eadb592bff576b2c8`;
manifest SHA256 is
`03edfda9b084dec6e1103c4e52c46ef836fa397c493ec9ddced397b7a17d2c33`.
Sources, runner, exact commands, outputs and earlier attempts remain external in
`es-v3-capacity-small-control-20260910`; fixed report is
`es-v3-capacity-small-fixed5-review-20260910.md`. Two separate control4 harness
faults detect unrelated XML changes and duplicate contributors with unchanged
counts; these are oracle-sensitivity checks, not production authority mutants.

No HTTP/DB/OCI/publication witness, four-owner retention, large graph/source/value
shape, dense child proof, simultaneous observation/transfer, original deadline or
whole-process resource qualification ran. The JVM's1GiB maximum is a test setting,
not a measured deployment allocation. Next is the actual hosted lifecycle/harness
and measured large retained shapes on a fixed artifact with assigned resources.
Do not replace the existing missing qualification with this small PASS.
