# Reuse renderer — implementation and verification

Base `3efe73df8a69819046cdee6be4174cbd154b991a`, application integration parent
`b74ffac25decd95f1207ea30ca2292faaf678bba`. Sole lead writer. Existing catalogue,
inventory and whole/partial reuse controllers remain the application authority.
This record is an unfinished slice, not integrated acceptance or release evidence.

Acceptance: preserve inspected-plan context; explicitly prepare an incomplete
target; select whole/partial structure; show complete required dependencies;
require explicit placement for each included item; preserve choices across pages;
apply only explicitly. An uncertain response retains its original command and
read-only context for explicit retry. Only an acknowledgement can show applied;
a separate fresh summary reports current target/export status. No donor values,
automatic matching, SQL execution or production publication are added.

## Actual author checks

External logs retain failures and corrections. Initial callable-empty renderer
failed its two operator tests. Explicit preparation RED showed selection exposed
before target preparation. Browser3 independently observed actual preview422
INCOMPLETE_TARGET; the prior HTTP workflow had explicitly materialized first.
The approved preparation action now calls materialization and refreshes context.

Browser4 completed the actual partial journey once at desktop and once at narrow:
real test-session capture/save/readback, explicit test-only publication witness,
UI target preparation, selection/preview/placement, command acknowledgement3,
fresh incomplete target/export-unavailable summary, return/logout and cleanup.
Axe/reflow/canary checks passed. This uses five freshly compiled test helpers ahead
of a hash-verified unchanged Java runtime. It is an author isolated mock workflow;
it does not qualify production compiler publication, databases or native resources.
External `es-v3-reuse-renderer-browser4-20260911/result.json` records exact hashes.

Browser5 deliberately lost delivery after the real original command returned200,
revision3. A real summary confirmed revision3 while the UI showed no success.
The test failed because Retry apply was absent. Recovery component red2 likewise
fails on missing Apply not confirmed. Red1 executed no test because the append
used an incorrect working-relative path; it is not behavior evidence. Recovery
implementation and its browser GREEN remain outstanding.

Renderer green7 passed seven focused tests: partial placement/acknowledgement,
whole reuse availability, source pagination, explicit preparation and lifetime
cases. Placement-pages2 independently passes21-item placement retention without
any command; pages1 was refused because its mock roots were not lexically sorted,
then the fixture was corrected to the contract. This is additional coverage of
existing behavior, not a claimed production RED. Check3 passed TypeScript/e2e
checking and Biome89files. The current suite intentionally retains the unresolved
recovery RED; there is no complete current frontend gate PASS.

## Independent source review and disposition

REUSE-UI-001 accepted: a retired catalogue chain could start an old inventory
read after context replacement/reentry. Author retired-chain-red1 reproduced one
unexpected old request. The correction binds the outer continuation to a distinct
API/context/presentation owner. Fixed snapshot2 review accepted that correction.

REUSE-UI-002 accepted: a completed preparation diagnostic survived plan changes.
Author preparation-context-red1 reproduced the old alert under a replacement
plan. Clear completed diagnostics on owner/context/presentation retirement,
retaining same-context rerenders. Green7 includes that distinction. Fixed review3
accepted the correction; manifest
`69990788113da6bfa45aeef06d2d9ea3f1b928ef1639f19cbd356c52df742da6`.
Reports: external `es-v3-reuse-lifetime-review2-20260911.md` and
`es-v3-reuse-preparation-review3-20260911.md`. Reviewer execution NONE.

Test-helper review1 verified four-file manifest
`e12cd6ba9df8e447533bd5a1463af8cf34f26cdc5e2563ab1b4b24c6e079cdb9` and found
no material harness defect. Test-only publication reads the actual saved capture,
appends historical revision2 and checks readback; the current witness is separate.
The actual server handles both command attempts. The review's stale “no API
interception” comment finding is accepted and corrected to describe delivery loss.
Report `es-reuse-harness-review1-20260911.md`; reviewer execution NONE.

## Presentation and remaining evidence

Six loaded-state and two prerequisite approvals are recorded in the reference
manifests. Four recovery/new-item extensions remain pending approval. Original
SVG/flat Midnight tokens/readable14–16px/44px controls are required corrections.
Browser4 captures were inspected against the original references: desktop density
and duplicate source heading needed correction; changes await recapture. Applied
read-only dependency detail and recovery remain unfinished. No pixel-identity,
comprehensive accessibility, full integrated browser or release claim is made.

Next: finish approved recovery after its pending image decision, verify exact
retry and original context across navigation, recapture corrected desktop/narrow
layouts and check keyboard/target sizes/reflow. Obtain independent fixed-candidate
review and all required integrated gates. The remote reviewer retains b74 full
build/browser/OCI ownership; its results exclude this WIP.

## Lead completion continuation — 11 September 2026

The user approved the four exact recovery/new-item proposals with their listed
corrections. Approval is recorded in `reuse-recovery-create-approval.json`.
The original 35-file paused WIP was hash-verified before transfer. Its worktree
remains preserved; the lead copied eligible work onto exact application base
`75f2af3762927389e6c1eb4fff69dc3d4f0e6d34` in
`/home/tim/.tmp/es-reuse-completion-20260911`, branch
`implementation/reuse-completion-20260911`. The shared workflow contract merged
cleanly with the newer validation section. No user or native work was discarded.

Recovery RED was rerun: 1 failed / 8 passed, because the recovery heading and
retry control were absent. The renderer now retains the original plan, source,
preview, included items, dependencies, inventory and decisions across uncertain
apply/reentry. It uses the existing exact-command retry hook and shows success
only after acknowledgement. Read-only current-item details share the placement
component, and the receipt distinguishes original from acknowledged revision.
The warning stays visible while retry is outstanding; no editor replaces it.

Actual author checks: 443 frontend tests and61 schema cases pass; TypeScript,
e2e typecheck, Biome and production build pass. Additional creation coverage
checks blank/invalid identifiers, no premature command and exact explicit choice.
No production RED is claimed for this additional preexisting creation coverage.

Fresh current-worktree backend test compilation passed. Browser6 and Browser7
each passed desktop1440x1000 and narrow390x844 actual HTTPS/OIDC/SQLite journeys:
real capture/save/readback, explicit test publication witness, materialization,
partial preview/placement, real command200 with deliberately lost delivery,
no false success, exact URL/body retry, acknowledged revision3, fresh incomplete
plan/export-unavailable status, logout/canary checks and confirmed cleanup.
Browser7 additionally inspected the truthful blank new-item state. Each state
passed axe, 44px target checks and320px reflow; keyboard activates preview.
These are bounded accessibility checks, not comprehensive WCAG certification.

Visual inspection corrected warning placement, repeated narrow selection rows,
stacked narrow read-only rows and clipped desktop creation labels. The latest
captures remain external, with approved references retained separately. Flat
tokens, actual SVG, readable text, natural scrolling and the compact shell are
preserved. Pixel identity is NOT claimed: generated raster dimensions differ,
and remaining typography, card-density and shell-layout differences require
explicit fidelity disposition. Functionality and visual fidelity are separate.

The existing actual runtime publication witness is test-only; production compiler
publication, external database/client qualification and export authority remain
closed. Lead self-review replaces no historical independent evidence. Reviewers
are retired by user instruction. New candidate integration/release gates must
refer to the exact candidate tested; prior remote build reservations are ended.
