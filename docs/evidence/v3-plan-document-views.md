# Hosted v3 document comparison and locations

The two POST routes in [the contract](../contracts/hosted-plan-document-views-v3.md)
now expose exact multi-document comparison and complete field/reference locations.
Both use the original pinned view and semantic transfer ownership. All three XML
modes and every location page require explicit full-document disclosure. The
existing compiler still refuses new v3 publication; these routes add no readiness.

## Fixed candidate and review

Base `f80eb2e1e55ea0622e2747fc0e718400c9fb430a`, combined twelve-file manifest
`74c824b424a27bcbe4d1ce251578be55d607108a6dc57e25ebd96f3bdd4fc644`: author5,
OpenAPI3 and lead HTTP/security4. Source hashes remain exact before/after the full
reactor. Lead reviewed all author files; the separate reviewer read OpenAPI3 and
root4 against fixed12, with no blocker. Its completed report was saved before an
agent usage-limit error, then independently read/hash-verified by the lead.
Report `es-v3-document-http-supplement-review-author-20260910.md` SHA
`a5b3ff8606f77115337847c0f2aeeef5f601f6ea464590a5abcb9a61deb50b16`.
Author journal SHA`4252a4ecb3da848a07387ee18d59cb3161d6704b769c8e203ab7b601f308461e`.

The production change adds only two closed dispatch cases, a controller and two
exact authenticated POST matchers. Existing readers, physical reply, proof adapters,
view pin/run lifetime and cleanup settlement remain unchanged. Current comparison
rechecks original proofs; target comparison also rechecks complete final proof.
Locations recheck original and any retained target. No lease-only fallback or
snapshot-only authority is introduced.

## Behavior and adverse evidence

Independently invented actual XML and explicit imported publication/observation
witnesses exercise the real compiler, content adapter, MockOIDC and socket transport.
Only MECHANISM_UNQUALIFIED remains in fixture compilation; no runtime witness is
registered or represented as actual publication readiness.

- Both current raw documents equal independent literals. Placeholders replace only
  declared attribute/child values and mapped references using returned stable
  identity tokens. Unmapped lookalikes remain exact. Formatted output is checked
  against an independently written display-only literal.
- SECRET XML text is returned only after explicit full-document consent; the
  binding rail stays masked. Missing/false/nonboolean consent refuses, including
  all document modes and empty/beyond-end locations.
- All three identity field/reference occurrences across two documents are paged
  in order, with complete totals, independent source digests and lexical slices.
  Child-value coordinates preserve escaped source spelling and exact UTF16 spans.
- HTTP commands change PUBLIC and child text; independently literal target XML
  preserves original text and the other document. Supplementary Unicode and XML
  escaping have exact target spans. Fresh child creation uses returned placement
  coordinates and has target-only locations; Fresh current refuses.
- Missing target, unknown field, stale/foreign owner, CSRF and malformed requests
  refuse. Controlled tests independently cover both V2 routes before body/capacity,
  admission rollback, removed Existing zero locations, stable separate Fresh tokens,
  and original pin loss during held output with no appended error.
- Actual partial document input retains scratch and semantic capacity. Other-owner
  metadata stays readable. Logout preserves both legal204/503 cleanup schedules;
  actual worker closure recovers capacity, and re-login cannot restore the plan.
  DEBUG logs and workspace files exclude synthetic credentials, tokens and secrets.

No new actual maximum unread-output, combined heap, database/client or browser
qualification is claimed. The reviewer read fixed source and evidence, without
claiming a separate socket or npm execution. Fixtures and vocabulary were reviewed
as independently invented; no private model or transformed private input is present.

## Actual checks

Pinned JDK21.0.12/Maven3.9.16/Node24.20.0,10 September2026:

| Check | Result |
| --- | --- |
| Lead actual HTTP RED | 3 assertion failures/0 errors for missing routes and absent held-transfer record |
| Lead actual HTTP GREEN | 24 tests:2 controls,22server including3 new socket and7 author tests; all pass04:17:10BST |
| Author focused suites | 42 and36 tests pass; latter includes legacy disclosure/location/child/comparison controls |
| Targeted mutations | Forced Raw mode, wrong location field and missing rollback each compile and fail1 assertion/0 errors; exact restored3 pass each |
| Full Maven verify | 1549:324core/7parser/908server/310supervisor,0 failures/errors/skips; assembly/hostile launch pass04:21:33BST |
| Fresh frontend check/test/build | 40frontend/55schema pass; preceding schema RED fails only2 missing-route assertions |

Full log `es-v3-plan-document-full1-20260910.log` SHA
`ff7f93e33c5dc0da8b49a77c3a9a3befe1f62db32bcdf2fead075f45fc22c031`.
Lead logs use `es-v3-document-http-independent-{red1,green1}-20260910.log`:
RED SHA`1625d4088a511704f8c301ed12d50b9630b1b69b94a08e9347216014c9abd272`,
GREEN SHA`f5fb70b656401b56d943616446f1f3f15b9872d6cea53228fa18dcdfbfcae821`.
OpenAPI green SHA`f5bf609484748e9232c1c70b0120d77136ad4ebfb2eca47d33907b51500f4fdc`.
The author corrected a literal lexical-length oracle from9 to10 without production
changes. Author initial RED/GREEN log paths collided with lead paths and were
overwritten; their observed tool results are retained in the author journal but
raw logs were not reconstructed. Final suites/mutation logs and lead RED/GREEN
remain available. A result-count script first used the wrong parser directory;
corrected enumeration covers the actual qualified-xml-parser module above.

G00 whole staged provenance/content guard, Python11, repository and diff checks
PASS during integration. Next are computed HTTP pages, capture/reuse and
validation. Operator UX, native execution/readback, combined resource proof,
current OCI/remote CI/GHCR/HiveForge and release qualification remain open.
