# Small HTTP v3 resource prerequisite — 10 September 2026

The external four-owner HTTP control passes against exact candidate
`39a9009fc93f22bb65a2eb992718c96cb67b0d0b`. Fixed non-author source review closes
two harness findings. This extends the [hosted service control](v3-hosted-capacity-prerequisite.md)
through actual HTTP, session and transfer ownership; it does not qualify deployed
capacity or publication.

## Acceptance and actual results

An actual random-port Spring context, mock OIDC exchange and schema3 SQLite
workspace drive production routes, services, content adapters and encoders.
Four owners each retain two exact current/target XML documents, 10 physical and
10 computed nodes, 50 memberships, 100 contributor links and 64 zero-pair rules.
Actual returned physical handles drive explicit target values. Every retained
plan is rechecked after later admissions. Independent observation fingerprints
distinguish owners whose mock XML is identical.

Checks cover complete original, decision and final pins; physical expectation;
typed preliminary inputs, decisions and computed proofs; final physical/input/
computed state; and every retained provenance entry. Expected preliminary records
come directly from the invented fixture and submitted decisions. Independent
expected sources/origins/field spans and contributor lists check final content.
Actual Raw responses preserve both complete documents on each side. Seven-row
validation pages cover all64 rules, partial/empty tails and exact fingerprint,
identity, bounds and outcome. Export remains unavailable. Fifth plan creation,
foreign plan access and stale validation fingerprints refuse.

A held actual validation request retains the original transfer record. A peer
semantic request refuses429 while metadata remains readable. Logout returns its
documented204/503, revokes the original session and drains owned work; the cancelled
request returns no JSON result. A fresh session for the same owner observes and
edits a replacement plan, then all four plans and pages pass again. Five started
mock observations each close transient credentials. Final cleanup attempts every
owner even after an earlier failure, verifies session denial/transfer drain and
checks mock credential canaries against captured logs and workspace files.
PASS follows all JUnit lifecycle checks, including workspace and context cleanup.

## Attribution and reviewed corrections

Frozen control4 javac and JVM exit0: one successful JUnit test, two successful
containers, no failures/skips, 5,099ms. Ubuntu JDK21.0.12 runs with a1GiB heap
maximum. The source-only reviewer executed no tests or builds.

The runner removes one inherited empty classpath component and rejects relative
entries before compilation, hashing or execution. Its final121 absolute entries
have1,583 hashed input files, checked before/after; external output classes are
separately hashed. These are the candidate's focused-build outputs, not a fresh
full build or OCI artifact. Result manifest SHA256:
`066a6026a74fb92a9cb7289957fd7885c2a84da09437f49e8d8a603e5a53db88`.
Configuration SHA256:
`beb3b3a190ed97af8e5d3fb1880c92a2973bc8bdd2d2339460948398956703ae`.
All sources, commands, results and attempts remain external in
`es-v3-http-capacity-small-20260910`; review reports are
`es-v3-http-capacity-small-review-20260910.md` and
`es-v3-http-capacity-small-fixed4-review-20260910.md`.

Control1 failed Java setup. Control2 incorrectly expected a plan-route header
refusal for an unauthenticated request; the security filter correctly returns its
JSON401. Control3 passed but independent review found unchecked preliminary/
physical evidence and full final pins, plus an empty classpath entry inventoried
against the wrong working directory. Control4 supplies independent full-record
oracles and the absolute classpath. Earlier attempts are preserved as harness
setup/oracle defects, not production TDD RED or killed production mutants.

Publication, observation and destination metadata are explicit mocks. The actual
compiler stays Incomplete/MECHANISM_UNQUALIFIED. No TLS, actual database cleanup,
packaged static assets, production entrypoint, large shape, simultaneous encoders
or measured whole-process/native capacity ran. The1GiB setting is not a deployment
allocation. An inspected artifact-port recipe identifies the next small TLS/
packaged-resource prerequisite; it is not a prepared command or assigned campaign.
