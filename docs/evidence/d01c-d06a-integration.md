# Native workspace and structural target integration — 8 September 2026

The integrated candidate adds owned native definition/profile revisions and
publication, explicit offline metadata upgrade, and complete cross-document
structural target materialization. Hosted plans, credential input, browser
publication/composition and guarded export remain unavailable. This evidence
does not declare the end-to-end product or either SQL client qualified.

The lead verified and copied 30 native-workspace candidate files byte-for-byte;
the shared server POM merge adds only the two inspection schema resources and
preserves both JDBC drivers. The worker's 31-file manifest SHA-256 is
`6d349e4ddb77f6307f1143f50ff18eb26d3892156c097c6316030e55266268d9`.
The 20 structural-target additions were copied byte-for-byte from manifest
`7dbd5abfea2ee14cef169570c8c73940beadd2060013e158cf0cd515d63a8a59`.
The structural fixture family has exact XML Git attributes and Docker build
inclusion. Docker's UI stage includes only the needed OpenAPI contract through
an explicit ignore-file exception for the historical-version schema test.

Both fixed candidates received non-authoring review. The workspace reviewer
independently passed 21 focused publication/request/SQLite/upgrade test methods,
six historical-mechanism schema assertions and an additional trailing-root
refusal challenge. The structural reviewer independently passed 29 planning,
adapter and assembly test methods including both-binding one-to-two goldens,
identity capture, qualified subtree moves, namespace/base context and capacity.
No material implementation finding remained. The historical response-schema
constant-version mismatch was corrected through an observed schema RED/GREEN;
new publication still requires exact supported versions. Full worker TDD,
mutation and limitations are in [workspace evidence](d01c-native-workspace.md)
and [structural evidence](d06a-structural-target.md).

Actual lead checks:

- `docker build --target runtime -t environment-studio:d01c-d06a-review .`
  passed the complete Maven reactor: **302 tests**, 94 core, 7 qualified parser,
  201 server; no failures, errors or skips. It also passed TypeScript checks,
  7 component tests, 14 contract tests and the production frontend build.
- `bash scripts/container_smoke.sh environment-studio:d01c-d06a-review` passed
  non-root/read-only/noexec startup, health/static UI, disabled demo mutation and
  inspection/export checks, private schema-2 initialization, permissions and
  exact overwrite refusal. The same harness passed actual offline schema-1
  upgrade and unchanged database bytes after repeated/extra-argument refusal.
- Repository integrity and all 10 Python script tests passed. The complete staged diff was reviewed for provenance; the content guard and
  exact XML-byte checks passed before commit. The independent reviewer checked
  all four staged XML files, including eight CRLF sequences in each glyph file.

Two direct root Maven attempts were not successful: generated test output
contained an extra class named `src.test.java.studio.environment.buildxml...`
with unresolved package errors. Moving generated outputs outside the checkout
and recompiling reproduced that class in the IDE-observed directory. Source files
and the qualified parser were unchanged. The isolated Docker build, excluding
host target directories, compiled and passed the complete reactor. The failed
root attempts are not counted as passing evidence; their logs are
`/tmp/es-d01c-integrated.log` and `/tmp/es-d01c-integrated-fresh.log`. Build-output
interference remains a local tooling limitation pending focused investigation.
The initial Docker attempt also exposed the docs ignore rule; its explicit
OpenAPI exception was added before the successful build, without broadening the
image to contain other documentation or private configuration.

The worker's 128-document/2.56-million-element test passed under a 256 MiB heap;
the reviewer and combined reactor reran its behavior with their normal heap,
so they do not independently repeat that heap qualification. Existing actual
D04 PostgreSQL 18.6/Oracle Free 23.26.3 observation evidence remains unchanged;
this integration did not repeat or claim new database/client qualification.

All new fixtures, metadata stores, principals and canaries were independently
invented. No actual application model, credentials or private evidence entered
the repository. The local runtime manifest is
`sha256:93125902c222de5af6c4d573eae7db9005410b1db9dbf30e849e9adb3ca392a5`;
the final documentation-metadata rebuild retained that exact runtime manifest
and config. Its local index is
`sha256:0d7c1291f0321bb8f8b9f1bf888ab2757f5744f3227fb12a4f87e66b66012824`.
The image is unpublished. Automatic approval review still
blocks the authorized push; no new PR, remote CI, GHCR or HiveForge result is
claimed. These are local development checks, not HiveGate execution authority.
Integration included the observed build-output and Docker-ignore rework; no
isolated active-time total or parallel speed-up is claimed.
