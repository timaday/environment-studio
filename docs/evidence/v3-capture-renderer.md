# Capture operator journey candidate

Base e0ab0af3dbe46619acc4d12cffb4667db30ca386; lead is sole implementation
owner. Connects the approved mapping, captured review, saved draft and original
session unknown-save recovery layouts to the existing capture controller and
backend. No publication authority, runtime fallback or native change.

Acceptance: valid inspection precedes mapping; every returned item needs an
explicit portable identifier and label; 20-row pagination preserves entered work.
Capture prepares value-free source without saving. A separate save creates the
draft; a lost response must not claim Not saved or Draft saved. Explicit retry
uses the original command, including after returning to the same plan and
reopening Capture. Pending edits remain unavailable. Saved state is an original
draft receipt, not publication or a fresh plan assertion. Session termination
retires owned state. No source identifier or donor field value is substituted
for an explicit portable mapping.

Original renderer TDD and earlier captures remain external. Recovery RED1 failed
on the missing Save not confirmed heading. Reentry RED1 then failed on the same
heading after Back to plan/reopen while the pending command survived. Both were
observed before their corrections. Final focused run: **13 tests PASS** across
V3CaptureJourney and captureSummary. Covers explicit capture/save separation,
complete/duplicate mappings, pagination, invalid inspection, unsaved invalidation,
compact navigation, exact retry with duplicate dispatch prevention, and strict
lossless numeric-revision projection. Original source bytes remain unchanged.

Actual affected browser workflow: **desktop 1 PASS, narrow 1 PASS**, each fresh
isolated HTTP/OIDC/SQLite fixture JVM. Returned inventory is mapped through the
renderer; actual capture is checked before explicit save. Original PUT reaches
the fixture server, browser delivery is aborted, unknown UI appears, Back/reopen
retains recovery, and keyboard retry returns the identical draft. Exact command,
capture bytes and historical readback match. No synthetic response substitution.
The committed e2e case contains this controlled path. Prior ordinary-save browser3
and recovery browser4 evidence remains scoped to those earlier source hashes.

Browser5 recorded mapping/review/recovery/saved and 320-CSS-pixel reflow captures;
axe reports no violations, inspected controls are at least 44 CSS pixels, and no
page overflow occurs. Tab/Enter reaches and toggles source disclosure and invokes
Retry save with a visible focus outline. This is bounded keyboard/reflow evidence,
not complete accessibility certification or actual browser-zoom measurement.
Each owned JVM reports cleanup complete; source, assets and inherited qualified
runtime hashes remain unchanged. Private reports/session material stay in RAM.

Commands: focused Vitest files; npm run check; npm run build; profiles-v3-renderer
Playwright desktop/narrow. Actual result/launch/runtime/source hashes are external
under es-v3-capture-renderer-browser5-20260911; latest check4/build6 and focused
green2 logs are retained. A Biome invocation from the wrong directory failed on
nested configuration; rerunning from frontend passed without changing config.

Design approvals: six v4 states and recovery desktop v1/narrow v3, with recorded
SVG/flat-token/readability/44px/scroll corrections. Independent visual review3
found earlier material browser2 issues addressed in browser3 and no additional
material recovery-proposal defect. Its pending-approval wording predates the user
approval; it is not renderer source review or runtime execution. Fidelity remains
DIFFERENCES_REMAIN: progress connectors, grouping/line wraps and expanded desktop
saved details differ. No pixel identity or blanket approved-exception claim.

Fixed source review, combined integration and required G00/G01/G02/G03/G08 remain
pending. Other-context and expired-session recovery presentation is not approved
by these images; original ownership must remain explicit before those states are
accepted. Native qualification, production publication and release remain blocked
by their own evidence. Only independently invented eligible fixtures were used.


## CAPTURE-UI-001 correction

Fixed non-author review1 verified all20files and found one medium issue: duplicate
portable IDs disable capture without explaining the collision across pages.
Lead accepted it. The enhanced21item test failed on the missing alert before the
correction; final13focused tests pass. Shared diagnostics identify the global
other-item number and associate errors with visible inputs; collapsed rows retain
an explicit marker. Mappings survive page navigation and errors clear on correction.
Grouping is linear in inventory size; only displayed rows render field messages.

Desktop/narrow duplicate diagnostic v2 proposals were approved with listed
corrections on11September (call_G5QcklItNMaj5x5z6YyLXE7W). Actual SVG/flat tokens,
14–16CSStext/44CSScontrols, pale-red text and exclusion of the generated stray
icon remain mandatory. Existing full-screen approvals are preserved. The first
narrow proposal incorrectly enabled capture and omitted diagnostic text; it was
revised before approval. No implementation was authorized from that failed proposal.

Latest check5/build7 pass. Browser6 desktop1/narrow1 pass with actual duplicate
entry/refusal/clearance plus capture/save/lost-response/reentry/exact retry/readback.
Axe/44px/reflow/keyboard checks and owned cleanup pass; source/assets/inherited
hashes unchanged. Immutable corrected non-author review2 found CAPTURE-UI-001 corrected with no
additional confirmed finding; all23hashes verified, execution NONE, manifest
54eed9e77dc2d58496df864926bcfcdb81b22f11b439f727342d0dcd1d78dff1.
External report es-capture-renderer-source-review2-20260911.md. This later status
update changes no source. Browser5 evidence
remains unchanged. Independent source
review1 execution NONE. Full integrated gates remain pending.
