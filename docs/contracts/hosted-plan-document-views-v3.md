# Hosted v3 document comparison and complete locations

This extends the hosted v3 physical/structural routes with POST /api/v3/plans/{planId}/views/document and /views/binding-locations. Preserve the original live lease, Host/Origin/CSRF, fixed V3 ownership and the existing full view scratch plus single semantic HTTP record before body access. Original ViewAdmission.run spans parsing, pinning, complete qualified comparison/scan, encoding, readiness, output, flush and final verification. Before pinning errors retain original reserved authority; after pinning every response, including errors, retains that same pin or aborts. No metadata pool, second scratch, physical permit, page cache, revision change, replay ID or new runtime qualification.

Reuse exact plan-view-v1.schema.json requests/responses and streaming reader. Document request is {revision,side,documentId,mode,completeDocumentDisclosure:true}; side current|target, mode raw|placeholders|formatted. Locations request is {revision,entity,fieldId,side,offset,limit,completeDocumentDisclosure:true}, with original Existing or explicit Fresh reference, canonical integer offset0..2147483647 and limit1..100. Every property is required. Reject duplicates/unknown properties, extra roots, noncanonical integers, reveal/model/result authority and missing/false/nonboolean disclosure before rendering, including empty/beyond-end requests. Full-document acknowledgement stays in the POST body; no GET/reveal alias or consent in URLs.

Document results exactly retain {revision,documentId,side,mode,text,exact,redacted,unmappedConcreteMayRemain,omissions}. Use original controlled ViewAdmission.document and the established plan-comparison-v3 semantics: current comparison independently verifies all original sources and proofs; target comparison additionally reconstructs and compares the complete actual target against original/decision pins, full draft and complete contributor evidence. No snapshot-only or v2-ready shortcut. Current retained content after failed inspection remains readable with unchanged invalid inspection authority. Missing target refuses INCOMPLETE_TARGET, never empty text or stale prior target. Existing four-Mi-character display limit, exact Raw characters and Formatted display-only behavior remain unchanged. Formatting never becomes writer input or mutates plan state.

Placeholders use stable opaque per-entity field tokens at every exact declared attribute or child-property mapping and every mapped reference to the target identity field. Unmapped content is preserved and may contain concrete/secret values. Explicit acknowledgement is required in all modes because entire document text and location offsets can disclose data beyond a selected readable field. Raw/Formatted preserve the existing mapped-unreadable-field refusal; this does not waive compiler qualification for unreadable definitions. Binding rail masking remains unchanged even after document disclosure. Computed groups have no invented XML positions.

Location pages use the existing controlled V3PlanPhysicalViews.locations with complete original and any retained target reproof and original cancellation. Return the existing {revision,total,offset,nextOffset,items} envelope and exact {documentId,sourceDigest,projectionId,elementIndex,attribute,span,role,declarationId} rows. UTF-16 spans select only actual lexical attribute values in the exact raw document; child-property entity/selector/location semantics follow the existing locator contract. Complete counts cover every field/reference occurrence across all documents with stable document/element/span/role/declaration ordering. Empty means a complete search: absent optional or removed materialized Existing has zero; Fresh current refuses NOT_FOUND, missing target INCOMPLETE_TARGET, unknown reference/field refuses. Every location page requires disclosure because offsets reveal lexical lengths, including lengths of earlier masked values.

Requests retain16KiB/depth4/128tokens/original10s limits. Replies retain128MiB with one original30s encoding/output/flush deadline, checked through readiness/write/flush and after rendering. No truncation, appended error after partial response, retained application page copy or authority replacement. Existing cancellation, worker closure, cleanup uncertainty and sole async settlement remain unchanged; bytes already handed to the container cannot be recalled. Current v3 compiler/publication stays incomplete; these routes and test witnesses confer no UI/export/client/resource availability.

Acceptance uses independently invented multi-document/child-property mappings where practical: exact Raw/Formatted/Placeholders before/after field/identity/Fresh edits, mapped-reference tokens stable across identity changes, unmapped lookalikes preserved, explicit full-document disclosure on all modes and empty locations, complete multiple field/reference coordinates slicing exact returned raw text, missing target/Fresh current/unknown fields, stale/foreign/V2/CSRF/closed-body refusals, original pin loss during held output and capacity recovery. Keep original qualified lower-level adversarial proof tests and unchanged v1 controls. No private XML/model or credentials enter repo/logs/images.
