# Hosted v3 materialization

Implementation target extending hosted-plan-http-v3 and hosted-plan-commands-v3. POST /api/v3/plans/{planId}/materializations takes the existing closed PlanViewReader revision-only request: {revision}, a canonical positive decimal string. No replay ID, model selector, target XML, caller graph or validation authority. Requires live original lease, Host/Origin/CSRF and fixed V3 ownership before body access, async start or admission. V2/foreign resources refuse404. Current v3 compilation/publication remain incomplete; test witnesses cannot enable runtime availability.

Reserve original full ViewAdmission scratch and the existing single semantic HTTP record before reading. Immediate admission; failure of the second acquisition closes the first. No additional metadata/physical slot or alternate scratch budget. Body remains16384 bytes, closed depth4/128-token grammar and original10s read deadline. Run this original view admission around parsing, revision pinning, actual materialization, result encoding, writable waits, writes, flush and final verification. The live run scope cannot end when the action merely returns a reply. Retain its scratch and HTTP record until actual application closure and sole async settlement. Existing terminal uncertainty/quarantine rules apply.

The response is exactly {revision, state, complete, diagnostics}; state is COMPLETE, INCOMPLETE or REFUSED and complete is true exactly for COMPLETE. Successful transport returns200 for all three explicit engine outcomes, without pretending refusal or incompleteness is a target. Core errors such as missing inspection, stale revision, missing/foreign plan or busy work retain their existing owned HTTP refusal mapping. Diagnostics carry the complete stable existing tool code or declared unresolved-reference strings; they are data, not safe-code headers, inferred application facts or validation PASS. No truncation. Use the existing full view response ceiling134217728 bytes rather than imposing the small Ack ceiling on all declared reference combinations. Encoding, output and flush share one original30s absolute deadline and original pinned-view verification at each bounded phase and after readiness. Before any output acquisition a failed response may yield the bounded closed error under that same deadline; after attempted output, abort without appending another response.

Materialization does not increment semantic revision or consume replay identity. It runs the existing v3 complete current/typed-target/final-XML proof pipeline and installs only its checked result; unresolved and refused states cannot fabricate target content. Original observed pins remain distinct from target pins. Forward original view cancellation to the v3 materializer and prevent installation after that cancellation; scratch remains held while the materializer is still executing. Session retirement and inspection/revision races are checked at the core and throughout reply transfer. Async completion never proves worker closure or peer receipt. Bytes or an installed result produced before a later transfer failure are not rolled back by HTTP.

Acceptance uses literal invented XML/current-derived counts and explicit test-only publication. Unchanged revision2 current materializes into complete equal XML with actual computed proof; unresolved intent returns INCOMPLETE with declared references; a refused materialization remains REFUSED. Check stale/missing inspection/version/owner/CSRF, both budgets independently, original rollback, malformed/partial body recovery, held materializer cancellation/no-late-target, retained scratch through held output, pinned authority checks through final flush, actual MockOIDC/CSRF sockets and current compiler refusal. No new comparison/capture/reuse/validation/export route is introduced by this single route.
