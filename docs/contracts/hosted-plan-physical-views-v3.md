# Hosted v3 document inventory and physical entities

Implementation target extending hosted-plan-materialization-v3 with two POST routes below /api/v3/plans/{planId}: /views/documents and /views/entities. It uses the existing controlled V3PlanPhysicalViews adapter, not v2 readiness or snapshot-only rendering. Actual compiler/publication qualification remains incomplete; explicit test witnesses confer no availability. No computed contributor, document text, profile, validation or export endpoint is introduced in this slice.

Both routes require original live lease, Host/Origin/CSRF and immutable V3 ownership before scratch/semantic HTTP record admission, body access or async setup. Reuse the existing single full ViewAdmission scratch plus one semantic HTTP record, without metadata slots, another scratch reservation or physical observation permit. Both immediate admissions precede reading; conclusively release the first if the second refuses. Retain original ViewAdmission.run through parsing, pinning, complete re-verification/rendering, encoding, writable waits, writes, flush and final verification. Early malformed/stale revision errors retain the original live reserved view/lease; after a pin exists every response including errors retains that pin or aborts. Original deadlines, cancellation, worker closure and sole async settlement follow the materialization/transfer contracts.

Requests reuse the exact closed plan-view-v1 request definitions and streaming reader. Documents takes {revision}; entities takes {revision, side, offset, limit}, with side current|target, canonical integer offset0..50000 and limit1..100. Revision is a canonical positive decimal string. No omitted defaults, extra root, duplicates, model selector, arbitrary filter, disclosure override or client result authority. Small body limit16384 bytes/depth4/128 tokens and original10s read deadline. These explicit read actions do not increment semantic revision or use command replay IDs; they may count as ordinary session activity. They are not replacement polling APIs.

Successful responses exactly retain existing v1 physical shapes in plan-view-v1.schema.json. Documents returns {revision,documents}, with complete ordered {documentId,currentDigest,targetDigest,changed} rows. Current inventory is complete; absent target digest and change are null, never unchanged. Entities returns {revision,total,offset,nextOffset,items}; each item is {entity,typeId,fields}, with fields {fieldId,present,masked,value}. Entity references are original opaque Existing handles or explicit Fresh slot/type references from actual selected provenance. They can subsequently be used in the existing typed commands; physical identities and computed keys are not invented handle values. Preserve stable ordering, whole-scope totals, page limit100 and null nextOffset when exhausted, including offsets beyond total. Missing target refuses INCOMPLETE_TARGET, not empty success.

Every request freshly verifies complete original content and any retained target through the controlled v3 adapter and original cancellation. Retained original content after failed inspection remains readable without restoring inspection/capture/export authority; concurrent inspection/revision/retirement changes invalidate the original view. Computed nodes are excluded from physical pages. Mask text exactly under declared readability and sensitivity: denied, SECRET and UNKNOWN values cannot appear, even if the browser requests them. Absent or masked values are null; empty present values remain distinct wherever the physical definition permits them. Document inventory does not reveal source XML or waive the separate future document/location disclosure requirement.

Full view responses use the existing134217728-byte encoder and one original30s absolute encoding/output/flush deadline. No truncation, retained page cache or queued whole-response copy. Original pinned authority is checked throughout encoding/transfer and after readiness; partial responses are aborted without an appended error. Typed returned data, application buffers and admission are released by the original worker before conclusive settlement. No claim of peer delivery or combined heap qualification follows from these transport counters.

Acceptance: actual MockOIDC/CSRF/socket inspection followed by full inventory and paged current entities; use an actual returned opaque handle in a PUBLIC command and see the target value while original stays exact. Verify target absent/null versus unchanged digest, complete totals and beyond-end pages, declared masking/absence and no computed leakage, wrong-version/foreign/stale/malformed requests, retained scratch/semantic record through held output and recovery by another owner. Existing controlled forged-proof/cancellation tests remain alongside new route tests. Preserve v1 bytes/behavior and current compiler refusal. All XML/definitions/principals are independently invented, with no private model retained in checkout, logs or evidence.
